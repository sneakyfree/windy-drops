{
  "name": "windy-drops",
  "version": "0.0.0",
  "private": true,
  "description": "Windy Drops — the open marketplace for the Windy ecosystem",
  "homepage": "https://windydrops.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/sneakyfree/windy-drops.git"
  },
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run codegen:check --workspaces --if-present",
    "build": "npm run build --workspaces",
    "test": "npm run build && npm run test --workspaces"
  }
}
